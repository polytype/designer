{% extends "layout.html.twig" %}

{% block content %}
    <h1>Frontpage</h1>
    
    <a href="{{ path('account_index', { 'accountName': 'example' } )}}" class="btn btn-lg btn-primary">Spaces in account 'example'</a>
    
{% endblock %}
